# 🏥 Hospital Management System — API Endpoints Reference

> Companion file to **`postman_collection.json`** (importable into Postman to test every endpoint).
>
> This document describes **what each endpoint does**, **who can call it**, and **how it is consumed on the client side** (which service function / page uses it and what behavior to expect).

---

## 1. Global Conventions

### Base URL

```
REST   → http://localhost:8080/api/v1   (env: NEXT_PUBLIC_API_URL / server PORT)
Health → http://localhost:8080/api/health
Auth   → http://localhost:8080/api/v1/auth  (Better Auth)
Socket → ws://localhost:8080  (Socket.IO real-time, FRD 41)
```

The Postman collection stores this as the `{{baseUrl}}` variable (`http://localhost:8080`).
On the server, the port is configurable via the `PORT` env var (default `5000` in `server/src/config/env.ts`; the project runs it on `8080`).

### Authentication

- **Mechanism:** Better Auth **session cookie** (`better-auth.session_token`), sent automatically with `credentials: "include"`.
- **Sign in** → server responds `Set-Cookie` → subsequent requests are authenticated automatically.
- **Sign out** requires the `Origin` header (CSRF protection) — the Postman collection sets `Origin: {{clientUrl}}` on sign-in/sign-out.
- **Roles:** `SUPER_ADMIN`, `HOSPITAL_ADMIN`, `DOCTOR`, `NURSE`, `LAB_TECHNICIAN`, `PATHOLOGIST`, `PHARMACIST`, `BILLING_STAFF`, `INVENTORY_MANAGER`, `AMBULANCE_DISPATCHER`, `AMBULANCE_DRIVER`, `WARD_BOY`, `ACCOUNTANT`, `IT_SUPPORT`, `RECEPTIONIST`, `PATIENT`.

### Response Envelope (FRD 3.3)

```jsonc
// Success
{ "success": true, "data": { ... }, "message": "optional", "meta": { "requestId": "...", "pagination": { "page": 1, "limit": 20, "totalItems": 0, "totalPages": 0 } } }

// Error
{ "success": false, "error": { "code": "NOT_FOUND", "message": "..." }, "meta": { "requestId": "..." } }
```

- **Pagination** is accepted via `?page=&limit=` query params on all list endpoints.
- **Better Auth endpoints** (`/auth/sign-in/*`, `/auth/sign-up/*`, `/auth/session`, …) do **not** use this envelope — they return Better Auth's native JSON.

### How the client talks to the API

All frontend calls go through **`client/src/shared/services/api.ts`**:

| Behavior | Detail |
|---|---|
| Transport | `fetch` with `credentials: "include"` (session cookie) |
| Base URL | `env.API_URL` from `client/src/shared/config/env.ts` → `NEXT_PUBLIC_API_URL` (default `http://localhost:8080/api/v1`) |
| Response | Unwraps `body.data` and returns it typed (`request<T>`) |
| Pagination | `api.list<T>()` surfaces `meta.pagination` as `{ items, pagination }` |
| Extra meta | `api.raw()` also returns `pagination` + `unread` (used by notifications) |
| Errors | Throws `ApiError { status, code, message, details }` from the error envelope |
| Auth | `better-auth/react` client in `client/src/shared/lib/auth-client.ts` (`signIn`, `signUp`, `signOut`, `useSession`); base URL `env.AUTH_URL` |

**Next.js proxy** (`client/src/proxy.ts`) validates the session cookie against `GET /api/v1/auth/get-session` before allowing portal routes (`/dashboard`, `/patients`, `/ipd`, …); stale cookies are cleared and the user is redirected to `/login`.

> **Testing tip:** In Postman, run **Auth → Sign In** first; the cookie is stored and all subsequent requests are authenticated. Replace `{{...}}` ID variables (e.g. `{{patientId}}`) with values returned by the corresponding **create** request.

---

## 2. Health

### `GET /api/health`
- **Purpose:** Liveness check — confirms the API is running.
- **Auth:** None (public).
- **Response:** `{ success, message, timestamp, uptime }`.
- **Client usage:** Not called by the UI; used by ops/monitoring and during local dev.

---

## 3. Auth (Better Auth + custom extensions) — `/api/v1/auth`

### Better Auth native endpoints (auto-generated by `better-auth`)

| Method | Path | Purpose | Client usage |
|---|---|---|---|
| `POST` | `/auth/sign-up/email` | Register a new patient/portal account (`{ name, email, password, phone?, dateOfBirth?, gender?, address?, bloodGroup? }`). Auto-signs in (`autoSignIn: true`). | `signUp.email()` in `auth-client.ts` (register page) |
| `POST` | `/auth/sign-in/email` | Sign in with email + password. Sets session cookie. | `signIn.email()` — login page |
| `POST` | `/auth/sign-out` | Destroy session. **Requires `Origin` header.** | `signOut()` — logout button |
| `GET` | `/auth/get-session` | Returns `{ session, user }` or `null`. | `useSession()` hook; **also used by the Next.js proxy** for route guarding |
| `GET` | `/auth/session` | Better Auth session introspection (alias). | `useSession()` |
| `GET` | `/auth/error` | OAuth error page fallback. | — |
| `POST` | `/auth/change-password` | Change password of the signed-in user. | Settings / profile |
| `POST` | `/auth/forget-password` | Request a password reset email. | Forgot password flow |
| `POST` | `/auth/reset-password` | Reset password with a token. | Reset password page |
| `POST` | `/auth/verify-email` | Verify email with a token. | Email verification |
| `POST` | `/auth/update-user` | Update own profile fields. | Profile editor |
| `DELETE` | `/auth/delete-user` | Delete own account. | Account settings |
| `POST` | `/auth/token` | Cookie → token exchange. | Server-side flows |
| `*` | `/auth/admin/*` | Admin plugin endpoints (list/create users, list sessions, revoke sessions, set role, ban/unban). | `adminClient()` plugin on the frontend |

### Custom auth endpoints

| Method | Path | Purpose | Auth / Roles | Client usage |
|---|---|---|---|---|
| `GET` | `/auth/me` | Current session + user (custom handler). | Any authenticated | Session bootstrap |
| `GET` | `/auth/sessions` | List the current user's active sessions. | Any authenticated | `listMySessions()` in `users.service.ts` — "Manage devices" UI |
| `DELETE` | `/auth/sessions/:sessionId` | Revoke one session (logout remote device). | Any authenticated | `revokeSession()` — device list |
| `GET` | `/auth/users?page=&limit=` | List all users (admin). | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `listAuthUsers()` — Users page |
| `POST` | `/auth/users` | Create a user with a role. `{ name, email, password, role, phone? }`. HOSPITAL_ADMIN can only create staff roles; SUPER_ADMIN can also create admins. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createAuthUser()` — "Add user" modal |

---

## 4. Users — `/api/v1/users`

| Method | Path | Purpose | Roles | Client usage (`users.service.ts`) |
|---|---|---|---|---|
| `GET` | `/users/me` | Fetch the authenticated user's own profile. | Any authenticated | `getMe()` — sidebar/header profile chip |
| `PATCH` | `/users/me` | Update own profile `{ name?, phone?, dateOfBirth?, gender?, address?, bloodGroup? }`. | Any authenticated | `updateMe()` — profile settings form |
| `POST` | `/users` | Create a user (admin). Body: `{ name, email, password, role, departmentId?, phone? }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createUser()` — Users page "Add user" |
| `GET` | `/users?page=&limit=&search=&role=` | List users with search/filter + pagination. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `listUsers()` — Users table |
| `GET` | `/users/:id` | Get one user. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `getUser()` — user detail/edit |
| `PATCH` | `/users/:id` | Update role/name/phone/active state. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `updateUser()` — edit dialog |
| `DELETE` | `/users/:id` | Deactivate a user (soft delete). | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `deactivateUser()` — deactivate action |
| `POST` | `/users/:id/avatar` | Upload avatar (multipart form-data, field `avatar`). ⚠️ **Backend stub → 501 `DEPENDENCY_FAILURE`** until S3 wiring; use `/uploads/presign` with `context: "AVATAR"` instead. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `uploadAvatar()` in `users.service.ts` (raw `fetch` with `FormData`) |
| `POST` | `/users/bulk-import` | Bulk create users. Body: `{ users: [...] }`. ⚠️ **Backend stub → 501** pending CSV job wiring. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `bulkImportUsers()` — "Bulk import" action (currently disabled on UI until backend lands) |

---

## 5. Roles — `/api/v1/roles`

| Method | Path | Purpose | Roles | Client usage (`users.service.ts`) |
|---|---|---|---|---|
| `GET` | `/roles/permissions` | List all assignable permission keys. | Any authenticated | `listPermissions()` — role editor permission checkboxes |
| `POST` | `/roles` | Create custom role `{ name, description?, permissionKeys? }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createRole()` |
| `GET` | `/roles` | List roles. | Any authenticated | `listRoles()` — roles table |
| `GET` | `/roles/:id` | Get one role. | Any authenticated | `getRole()` |
| `PATCH` | `/roles/:id` | Update role name/description/permissions. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `updateRole()` |
| `DELETE` | `/roles/:id` | Delete role. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `deleteRole()` |

---

## 6. Departments — `/api/v1/departments`

| Method | Path | Purpose | Roles | Client usage (`org.service.ts`) |
|---|---|---|---|---|
| `POST` | `/departments` | Create department `{ name, code, description?, hodUserId? }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createDepartment()` — Departments page |
| `GET` | `/departments?search=&isActive=` | List departments. | Any authenticated | `listDepartments()` — dropdowns (appointment booking, OPD queue filter) |
| `GET` | `/departments/:id/doctors` | Doctors assigned to a department. | Any authenticated | `listDepartmentDoctors()` — department detail / doctor filter |
| `GET` | `/departments/:id` | Get one department. | Any authenticated | `getDepartment()` |
| `PATCH` | `/departments/:id` | Update department. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `updateDepartment()` |
| `DELETE` | `/departments/:id` | Deactivate department. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `deactivateDepartment()` |

---

## 7. Doctors — `/api/v1/doctors`

| Method | Path | Purpose | Roles | Client usage (`org.service.ts`) |
|---|---|---|---|---|
| `POST` | `/doctors` | Create doctor profile `{ userId, departmentId, registrationNo, specialization, qualifications?, consultationFee?, experienceYears?, bio? }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createDoctor()` — Doctors page |
| `GET` | `/doctors?search=&departmentId=&specialization=` | List doctors. | Any authenticated | `listDoctors()` — doctor directory, booking dropdowns |
| `GET` | `/doctors/:id` | Get doctor profile. | Any authenticated | `getDoctor()` |
| `GET` | `/doctors/:id/slots?date=YYYY-MM-DD` | Generate available time slots for a date (from availability/leave). | Any authenticated | `getDoctorSlots()` — booking page slot picker |
| `PATCH` | `/doctors/:id` | Update profile/consultation fee. | `SUPER_ADMIN`, `HOSPITAL_ADMIN`, `DOCTOR` (self) | `updateDoctor()` |
| `POST` | `/doctors/:id/availability` | Set weekly availability `{ weekday, startTime, endTime, slotDurationMinutes?, clinicRoom? }`. | Admin or the doctor | `setDoctorAvailability()` — availability editor |
| `POST` | `/doctors/:id/leave` | Mark leave `{ startDate, endDate, reason? }` (blocks slots). | Admin or the doctor | `markDoctorLeave()` — leave calendar |
| `DELETE` | `/doctors/:id` | Deactivate doctor profile. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `deactivateDoctor()` |

---

## 8. Patients — `/api/v1/patients`

| Method | Path | Purpose | Roles | Client usage (`patients.service.ts`) |
|---|---|---|---|---|
| `POST` | `/patients` | Register patient `{ name, dob, gender, phone, email?, bloodGroup?, address?, emergencyContact?, allergies?, chronicConditions?, guardianPatientId? }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN`, `RECEPTIONIST` | `registerPatient()` — reception registration form |
| `GET` | `/patients/me` | Portal patient's own record. | Any authenticated | `getPatientMe()` — patient portal "My records" |
| `GET` | `/patients?search=&phone=&uhid=&page=&limit=` | Search/list patients (by name, phone, UHID). | `SUPER_ADMIN`, `HOSPITAL_ADMIN`, `RECEPTIONIST`, `DOCTOR`, `NURSE` | `searchPatients()` — patient directory + search-as-you-type |
| `GET` | `/patients/:id` | Full patient detail (history, visits). | Clinical staff + reception | `getPatient()` — patient detail page |
| `PATCH` | `/patients/:id` | Update demographics / medical history. | `SUPER_ADMIN`, `HOSPITAL_ADMIN`, `DOCTOR`, `NURSE` | `updatePatient()` |
| `POST` | `/patients/:id/documents` | Attach a document `{ docType: "ID_PROOF"\|"INSURANCE"\|"OTHER", s3Key }` (upload via `/uploads/presign` first). | Admin, `RECEPTIONIST`, `DOCTOR` | `addPatientDocument()` — document drawer |
| `GET` | `/patients/:id/timeline` | Chronological activity timeline for a patient. | Any authenticated | `getPatientTimeline()` — timeline tab |
| `POST` | `/patients/merge` | Merge duplicate records `{ survivingPatientId, mergedPatientId, reason }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `mergePatients()` — dedupe tool |

---

## 9. Appointments — `/api/v1/appointments`

Appointment lifecycle: **request → approve/reject → book → check-in → complete**. A `PATIENT` creates a `PENDING` request (their own Patient record is resolved server-side; they can never pick another patient). Receptionist/admin verifies doctor availability, leave, and slot, then `approve` → status `BOOKED` (slot reserved via unique `slotKey`) or `reject` with a `decisionNote`. Both the patient and the doctor are notified on approval/rejection.

| Method | Path | Purpose | Roles | Client usage (`appointments.service.ts`) |
|---|---|---|---|---|
| `POST` | `/appointments` | Create appointment `{ patientId?, doctorId, departmentId, appointmentDate, slotStartTime, slotEndTime, mode, reason? }`. `patientId` is ignored for `PATIENT` (own record used); their row is created `PENDING`. Staff rows are created `BOOKED` directly. | Admin, `RECEPTIONIST`, `PATIENT` | `bookAppointment()` — staff booking + patient request form |
| `GET` | `/appointments?status=&doctorId=&departmentId=&date=&search=` | List appointments with filters + pagination. `PATIENT` is scoped to their own record, `DOCTOR` to their own appointments. Date/slot must be in the future. | Any authenticated | `listAppointments()` — appointments table |
| `GET` | `/appointments/queue?doctorId=&date=` | Live queue for a doctor/day. | Any authenticated | `getAppointmentQueue()` — OPD queue screen |
| `GET` | `/appointments/:id` | Appointment detail. `PATIENT` only sees their own. | Any authenticated | `getAppointment()` |
| `PATCH` | `/appointments/:id/approve` | Approve a `PENDING` request `{ decisionNote? }` → re-checks doctor leave + slot availability, sets status `BOOKED`, notifies patient + doctor. | Admin, `RECEPTIONIST` | `approveAppointment()` — pending-requests queue |
| `PATCH` | `/appointments/:id/reject` | Reject a `PENDING` request `{ decisionNote }` → status `REJECTED`, notifies patient with the reason. | Admin, `RECEPTIONIST` | `rejectAppointment()` — pending-requests queue |
| `PATCH` | `/appointments/:id/reschedule` | Reschedule `{ appointmentDate, slotStartTime, slotEndTime }`. Patient reschedule returns the row to `PENDING` for re-approval; staff reschedule books the new slot directly. New date/slot must be in the future. | Admin, `RECEPTIONIST`, `PATIENT` | `rescheduleAppointment()` |
| `PATCH` | `/appointments/:id/cancel` | Cancel `{ reason? }`. `PATIENT` may only cancel their own. | Admin, `RECEPTIONIST`, `PATIENT` | `cancelAppointment()` |
| `POST` | `/appointments/:id/check-in` | Check patient in (marks arrival, moves to OPD queue). Requires `BOOKED`. | Admin, `RECEPTIONIST` | `checkInAppointment()` — reception action |
| `PATCH` | `/appointments/:id/complete` | Mark done: `BOOKED` / `CHECKED_IN` → `COMPLETED`. `DOCTOR` may only complete their own. | Admin, `RECEPTIONIST`, `DOCTOR` | `completeAppointment()` — mark-done action |

Statuses: `PENDING` (awaiting approval) → `BOOKED` → `CHECKED_IN` → `COMPLETED`, with terminal `REJECTED` / `CANCELLED` and `NO_SHOW` / `NEEDS_RESCHEDULE`.

---

## 10. OPD (Outpatient) — `/api/v1/opd`

| Method | Path | Purpose | Roles | Client usage (`appointments.service.ts`) |
|---|---|---|---|---|
| `GET` | `/opd/queue?departmentId=&doctorId=&date=` | Live OPD waiting queue. | Any authenticated | `getOpdQueue()` — OPD queue board |
| `POST` | `/opd/visits/:appointmentId/vitals` | Record vitals for a visit `{ bpSystolic?, bpDiastolic?, pulse?, temperature?, spo2?, weight?, height? }`. | `NURSE`, `DOCTOR`, admin | `recordVitals()` — vitals form |
| `GET` | `/opd/visits/:id` | Fetch the OPD visit (status, vitals, diagnosis). | Nurse/Doctor/Receptionist/admin | `getOpdVisit()` — consultation screen |
| `PATCH` | `/opd/visits/:id/start-consultation` | Doctor starts the consultation (status → IN_CONSULTATION). | `DOCTOR`, admin | `startConsultation()` |
| `PATCH` | `/opd/visits/:id/diagnosis` | Save diagnosis `{ icd10Code, description?, notes? }`. | `DOCTOR`, admin | `saveDiagnosis()` — diagnosis form |
| `PATCH` | `/opd/visits/:id/close` | Close the visit. | `DOCTOR`, admin | `closeOpdVisit()` |
| `POST` | `/opd/visits/:id/refer-ipd` | Refer OPD patient to IPD `{ reason?, wardType? }` → returns `{ admissionId }`. | `DOCTOR`, admin | `referToIpd()` — "Admit" action |

---

## 11. IPD (Inpatient) — `/api/v1/ipd`

| Method | Path | Purpose | Roles | Client usage (`ipd.service.ts`) |
|---|---|---|---|---|
| `POST` | `/ipd/admissions` | Admit patient `{ patientId, admittingDoctorId, wardId, bedId, admissionType, referredFromVisitId? }`. | Admin, `DOCTOR`, `RECEPTIONIST` | `admitPatient()` — admission form |
| `GET` | `/ipd/admissions?status=&wardId=&search=` | List admissions. | Admin, `DOCTOR`, `NURSE`, `RECEPTIONIST` | `listAdmissions()` — IPD census table |
| `GET` | `/ipd/admissions/:id` | Admission detail. | Same as above | `getAdmission()` |
| `POST` | `/ipd/admissions/:id/rounds` | Add doctor's round note `{ doctorId?, notes }`. | `DOCTOR`, admin | `addIpdRound()` — rounds timeline |
| `POST` | `/ipd/admissions/:id/vitals` | Chart vitals (same shape as OPD vitals). | `NURSE`, admin | `chartIpdVitals()` — vitals chart |
| `POST` | `/ipd/admissions/:id/transfer` | Transfer to another bed `{ toBedId, reason? }`. | Admin, `DOCTOR`, `RECEPTIONIST` | `transferAdmission()` |
| `PATCH` | `/ipd/admissions/:id/discharge` | Discharge patient `{ doctorId?, diagnosis?, dischargeType? }`. | Admin, `DOCTOR` | `dischargePatient()` |
| `GET` | `/ipd/admissions/:id/discharge-summary` | Fetch generated discharge summary. | Admin, `DOCTOR` | `getDischargeSummary()` |

---

## 12. Wards — `/api/v1/wards`

| Method | Path | Purpose | Roles | Client usage (`ipd.service.ts`) |
|---|---|---|---|---|
| `POST` | `/wards` | Create ward `{ name, wardType, departmentId?, floor, nursePatientRatio? }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createWard()` — Wards page |
| `GET` | `/wards` | List wards. | Any authenticated | `listWards()` — ward list + admission dropdown |
| `GET` | `/wards/:id` | Ward detail. | Any authenticated | `getWard()` |
| `GET` | `/wards/:id/census` | Ward census (occupancy, bed status breakdown). | Any authenticated | `getWardCensus()` — ward dashboard |
| `PATCH` | `/wards/:id` | Update ward. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `updateWard()` |
| `DELETE` | `/wards/:id` | Deactivate ward. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `deactivateWard()` |

---

## 13. Beds — `/api/v1/beds`

| Method | Path | Purpose | Roles | Client usage (`ipd.service.ts`) |
|---|---|---|---|---|
| `POST` | `/beds` | Create bed `{ wardId, bedNumber, bedType }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createBed()` — Beds page |
| `GET` | `/beds?wardId=&status=&search=` | List beds. | Any authenticated | `listBeds()` — bed board |
| `GET` | `/beds/:id` | Bed detail. | Any authenticated | `getBed()` |
| `PATCH` | `/beds/:id/status` | Change status `{ status: AVAILABLE\|OCCUPIED\|CLEANING\|MAINTENANCE\|RESERVED }`. | Admin, `NURSE`, `WARD_BOY` | `changeBedStatus()` — bed status actions |
| `DELETE` | `/beds/:id` | Remove bed. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `removeBed()` |

---

## 14. Ambulance — `/api/v1/ambulance`

| Method | Path | Purpose | Roles | Client usage (`ambulance.service.ts`) |
|---|---|---|---|---|
| `POST` | `/ambulance/vehicles` | Add vehicle `{ registrationNo, type, driverId? }`. | Admin, `AMBULANCE_DISPATCHER` | `addVehicle()` — fleet management |
| `GET` | `/ambulance/vehicles?status=` | List fleet. | Any authenticated | `listVehicles()` |
| `POST` | `/ambulance/requests` | Raise dispatch request `{ patientId?, pickupAddress, pickupLat?, pickupLng?, dropAddress?, urgency }`. | Admin, `AMBULANCE_DISPATCHER`, `RECEPTIONIST` | `raiseRequest()` — request form |
| `GET` | `/ambulance/requests?status=&urgency=` | List requests. | Admin, `AMBULANCE_DISPATCHER` | `listRequests()` — dispatch board |
| `PATCH` | `/ambulance/requests/:id/assign` | Assign vehicle/driver `{ vehicleId, driverId? }`. | Admin, `AMBULANCE_DISPATCHER` | `assignVehicle()` |
| `PATCH` | `/ambulance/trips/:id/status` | Driver updates trip status (e.g. ON_ROUTE → COMPLETED). | Admin, `AMBULANCE_DRIVER` | `updateTripStatus()` — driver app |
| `GET` | `/ambulance/trips/:id/track` | Live trip tracking (location/status). | Dispatcher/Driver/Receptionist/admin | `trackTrip()` — live map |

---

## 15. Prescriptions — `/api/v1/prescriptions`

| Method | Path | Purpose | Roles | Client usage (`clinical.service.ts`) |
|---|---|---|---|---|
| `POST` | `/prescriptions/interactions-check` | Drug interaction check `{ drugIds: string[] }`. | `DOCTOR`, admin | `checkInteractions()` — warning banner while prescribing |
| `POST` | `/prescriptions` | Create prescription `{ patientId, doctorId, opdVisitId?, ipdAdmissionId?, notes?, items: [{ drugId, dosage, frequency, durationDays, route?, instructions? }] }`. | `DOCTOR`, admin | `createPrescription()` — prescription form |
| `GET` | `/prescriptions?patientId=&status=` | List prescriptions. | Any authenticated | `listPrescriptions()` — history |
| `GET` | `/prescriptions/:id` | Prescription detail. | Any authenticated | `getPrescription()` |
| `GET` | `/prescriptions/:id/pdf` | Prescription PDF (returns `{ url }`). | Any authenticated | `getPrescriptionPdf()` — print/download |
| `POST` | `/prescriptions/:id/renew` | Renew a prescription. | `DOCTOR`, admin | `renewPrescription()` |

> ⚠️ **Known contract limitation (noted in the code):** there is no public drug-master catalog endpoint yet. The prescription form sends a free-text drug name as `drugId` until a catalog endpoint is added.

---

## 16. AI Prescriptions — `/api/v1/ai-prescriptions`

| Method | Path | Purpose | Roles | Client usage (`clinical.service.ts`) |
|---|---|---|---|---|
| `POST` | `/ai-prescriptions/suggest` | Ask AI to draft a prescription `{ patientId, doctorId, opdVisitId?, diagnosisText, symptoms? }`. | `DOCTOR`, admin | `suggestAiPrescription()` — "AI assist" button |
| `GET` | `/ai-prescriptions/:id` | Get a suggestion. | `DOCTOR`, admin | `getAiSuggestion()` |
| `POST` | `/ai-prescriptions/:id/accept` | Accept the suggestion (creates the prescription). | `DOCTOR`, admin | `acceptAiSuggestion()` |
| `PATCH` | `/ai-prescriptions/:id/edit` | Edit suggestion items/diagnosis before accepting. | `DOCTOR`, admin | `editAiSuggestion()` |
| `POST` | `/ai-prescriptions/:id/reject` | Reject suggestion `{ reason? }`. | `DOCTOR`, admin | `rejectAiSuggestion()` |

---

## 17. Laboratory — `/api/v1/lab`

| Method | Path | Purpose | Roles | Client usage (`clinical.service.ts`) |
|---|---|---|---|---|
| `GET` | `/lab/tests?search=&category=` | Lab test catalog. | Any authenticated | `listLabTests()` — test picker |
| `POST` | `/lab/orders` | Create lab order `{ patientId, doctorId, opdVisitId?, ipdAdmissionId?, testIds }`. | `DOCTOR`, admin | `createLabOrder()` |
| `GET` | `/lab/orders?status=&patientId=` | List lab orders. | Any authenticated | `listLabOrders()` |
| `PATCH` | `/lab/orders/:id/collect-sample` | Mark sample collected. | Admin, `LAB_TECHNICIAN` | `collectSample()` |
| `PATCH` | `/lab/orders/:id/results` | Enter results `{ results: [{ testParameterId, value }] }`. | `LAB_TECHNICIAN`, admin | `enterResults()` |
| `PATCH` | `/lab/orders/:id/verify` | Verify results. | `PATHOLOGIST`, admin | `verifyLabResults()` |
| `POST` | `/lab/orders/:id/release` | Release final report to patient/doctor. | `PATHOLOGIST`, `LAB_TECHNICIAN`, admin | `releaseReport()` |

---

## 18. Pharmacy — `/api/v1/pharmacy`

| Method | Path | Purpose | Roles | Client usage (`pharmacy.service.ts`) |
|---|---|---|---|---|
| `GET` | `/pharmacy/queue?departmentId=` | Pending dispensing queue. | Any authenticated | `getPharmacyQueue()` — pharmacy dashboard |
| `GET` | `/pharmacy/batches/suggest?drugId=&quantity=` | Suggest FEFO/FEFO batches to dispense from. | Any authenticated | `suggestBatches()` — dispense dialog |
| `POST` | `/pharmacy/dispenses` | Dispense `{ prescriptionId, items: [{ prescriptionItemId, drugId, batchId?, quantityDispensed }] }`. | `PHARMACIST`, admin | `createDispense()` |
| `GET` | `/pharmacy/dispenses/:id` | Dispense detail. | Any authenticated | `getDispense()` |
| `POST` | `/pharmacy/dispenses/:id/substitute` | Record a substitution `{ prescriptionItemId, drugId, batchId?, quantityDispensed, reason }`. | `PHARMACIST`, admin | `recordSubstitution()` |
| `POST` | `/pharmacy/returns` | Process a return `{ dispenseItemId, quantityReturned, reason }`. | `PHARMACIST`, admin | `processReturn()` |

---

## 19. Inventory — `/api/v1/inventory`

| Method | Path | Purpose | Roles | Client usage (`pharmacy.service.ts`) |
|---|---|---|---|---|
| `POST` | `/inventory/items` | Create item `{ name, category: DRUG\|CONSUMABLE\|EQUIPMENT, unit?, reorderPoint?, reorderQuantity? }`. | Admin, `INVENTORY_MANAGER` | `createItem()` |
| `GET` | `/inventory/items?search=&category=` | List items. | Any authenticated | `listItems()` |
| `POST` | `/inventory/batches` | Receive stock batch `{ itemId, batchNo, expiryDate, supplierId?, quantity, unitCost? }`. | Admin, `INVENTORY_MANAGER` | `receiveBatch()` |
| `GET` | `/inventory/stock?search=&lowStock=` | Current stock levels. | Any authenticated | `getStock()` — stock dashboard |
| `POST` | `/inventory/adjustments` | Adjust stock `{ itemId, batchId?, quantityDelta, reason? }`. | Admin, `INVENTORY_MANAGER` | `adjustStock()` |
| `POST` | `/inventory/suppliers` | Create supplier. | Admin, `INVENTORY_MANAGER` | `createSupplier()` |
| `GET` | `/inventory/suppliers?search=` | List suppliers. | Any authenticated | `listSuppliers()` |
| `POST` | `/inventory/purchase-orders` | Create PO `{ supplierId, items: [{ itemId, quantityOrdered, unitCost? }] }`. | Admin, `INVENTORY_MANAGER` | `createPurchaseOrder()` |
| `GET` | `/inventory/purchase-orders?status=` | List POs. | Any authenticated | `listPurchaseOrders()` |
| `PATCH` | `/inventory/purchase-orders/:id/receive` | Receive a PO (updates stock) `{ items: [{ itemId, quantityReceived }] }`. | Admin, `INVENTORY_MANAGER` | `receivePurchaseOrder()` |
| `GET` | `/inventory/alerts` | Low-stock / expiring alerts → returns grouped object `{ lowStock: [...] }`. | Any authenticated | `getInventoryAlerts()` — alert banner |

---

## 20. Billing — `/api/v1/billing`

| Method | Path | Purpose | Roles | Client usage (`billing.service.ts`) |
|---|---|---|---|---|
| `POST` | `/billing/bills` | Create bill `{ patientId, opdVisitId?, ipdAdmissionId?, ambulanceTripId?, items: [{ sourceModule, sourceReferenceId?, description, quantity?, unitPrice }] }`. | Admin, `BILLING_STAFF` | `createBill()` — billing desk |
| `GET` | `/billing/bills?status=&patientId=&search=` | List bills. | Admin, `BILLING_STAFF` | `listBills()` |
| `GET` | `/billing/bills/:id` | Bill detail (also patient-visible). | Admin, `BILLING_STAFF`, `PATIENT` | `getBill()` — patient "My bills" |
| `PATCH` | `/billing/bills/:id/discount` | Apply discount `{ percentage, reason? }`. | Admin, `BILLING_STAFF` | `applyDiscount()` |
| `POST` | `/billing/bills/:id/discount/approve` | Approve/reject a pending discount `{ approve, reason? }` (above threshold). | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `approveDiscount()` — admin approval queue |
| `POST` | `/billing/insurance-policies` | Create policy `{ patientId, providerName, policyNo, coverageLimit, validTill }`. | Admin, `BILLING_STAFF` | `createInsurancePolicy()` |
| `PATCH` | `/billing/bills/:id/insurance` | Apply insurance `{ policyId, amount? }`. | Admin, `BILLING_STAFF` | `applyInsurance()` |
| `PATCH` | `/billing/bills/:id/finalize` | Finalize bill (locks it for payment). | Admin, `BILLING_STAFF` | `finalizeBill()` |
| `POST` | `/billing/bills/:id/credit-note` | Issue credit note `{ amount, reason }`. | Admin, `BILLING_STAFF` | `issueCreditNote()` |

---

## 21. Payments — `/api/v1/payments`

| Method | Path | Purpose | Roles | Client usage (`billing.service.ts`) |
|---|---|---|---|---|
| `POST` | `/payments` | Record a cash/offline payment `{ billId, amount, mode, gatewayTransactionId? }`. | Admin, `BILLING_STAFF` | `recordPayment()` — cash counter |
| `POST` | `/payments/initiate-gateway` | Start an online gateway payment `{ billId, amount, mode? }` → returns `{ paymentUrl, transactionId }`. | Admin, `PATIENT` | `initiateGatewayPayment()` — patient "Pay online" |
| `POST` | `/payments/webhook` | Payment gateway callback. **Unauthenticated** (HMAC verified in handler). | Public (HMAC) | Not called by the app; gateway calls it |
| `POST` | `/payments/:id/refund` | Refund a payment `{ amount, reason }`. | Admin, `BILLING_STAFF` | `processRefund()` |
| `GET` | `/payments/reconciliation?date=` | Cash reconciliation summary for a date. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `getReconciliation()` — day-end report |
| `POST` | `/payments/reconciliation` | Create reconciliation entry `{ date, countedAmount }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createReconciliation()` |

---

## 22. Reports — `/api/v1/reports`

| Method | Path | Purpose | Roles | Client usage (`reports.service.ts`) |
|---|---|---|---|---|
| `GET` | `/reports/templates?module=` | List report templates. | Any authenticated | `listReportTemplates()` — report picker |
| `POST` | `/reports/generate` | Kick off async report job `{ templateId, params? }` → returns `{ jobId }`. | Admin, `BILLING_STAFF`, `INVENTORY_MANAGER`, `DOCTOR` | `generateReport()` |
| `GET` | `/reports/jobs/:id` | Poll job status + download when ready. | Any authenticated | `getReportJobStatus()` — progress poll |
| `POST` | `/reports/schedule` | Schedule recurring report `{ templateId, cronExpression, recipients?, params? }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createReportSchedule()` |
| `GET` | `/reports/schedule` | List schedules. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `listReportSchedules()` |

---

## 23. Notifications — `/api/v1/notifications`

| Method | Path | Purpose | Roles | Client usage (`notifications.service.ts`) |
|---|---|---|---|---|
| `GET` | `/notifications?page=&limit=` | Own inbox. Response meta includes `unread` count. | Any authenticated | `listNotifications()` — bell dropdown (uses `api.raw()` to read `meta.unread`) |
| `PATCH` | `/notifications/:id/read` | Mark one notification read. | Any authenticated | `markNotificationRead()` |
| `PATCH` | `/notifications/read-all` | Mark all read → `{ updated }`. | Any authenticated | `markAllNotificationsRead()` |
| `GET` | `/notifications/preferences` | List channel/category preferences. | Any authenticated | `getNotificationPreferences()` |
| `PATCH` | `/notifications/preferences` | Update preferences (array of `{ category, channel, isEnabled }`). | Any authenticated | `updateNotificationPreferences()` |
| `GET` | `/notifications/templates` | List notification templates. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `listNotificationTemplates()` |
| `POST` | `/notifications/templates` | Create template `{ key, channel, subject?, bodyTemplate, isCritical? }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `createNotificationTemplate()` |

---

## 24. Staff Chat — `/api/v1/chat` (internal staff only)

| Method | Path | Purpose | Roles | Client usage (`chat.service.ts`) |
|---|---|---|---|---|
| `POST` | `/chat/conversations` | Create DM/group `{ type?, title?, participantIds, linkedEntityType?, linkedEntityId? }`. | All staff roles (not `PATIENT`) | `createConversation()` |
| `GET` | `/chat/conversations` | List my conversations. | All staff roles | `listConversations()` — chat sidebar |
| `GET` | `/chat/conversations/:id/messages` | Message history (paginated). | All staff roles | `getChatMessages()` |
| `POST` | `/chat/conversations/:id/messages` | Send message `{ content }`. | All staff roles | `sendChatMessage()` |
| `PATCH` | `/chat/messages/:id` | Edit message `{ content }`. | Author | `editChatMessage()` |
| `POST` | `/chat/conversations/:id/read` | Mark conversation read. | Participant | `markConversationRead()` |

> **Real-time:** message delivery is also pushed over **Socket.IO** (see `server/src/core/utils/socket.ts`) — the REST endpoints above seed state; the socket layer (`chat.socket.ts`) streams new messages to connected clients.

---

## 25. Patient Chat — `/api/v1/patient-chat`

| Method | Path | Purpose | Roles | Client usage (`chat.service.ts`) |
|---|---|---|---|---|
| `POST` | `/patient-chat/conversations` | Patient starts a support conversation `{ departmentId? }`. | `PATIENT`, admin | `startPatientConversation()` |
| `GET` | `/patient-chat/conversations` | List conversations (patient: own; staff: assigned). | Any authenticated | `listPatientConversations()` |
| `GET` | `/patient-chat/conversations/:id/messages` | Message history. | Participant | `getPatientChatMessages()` |
| `POST` | `/patient-chat/conversations/:id/messages` | Send message `{ content }`. | `PATIENT`, `DOCTOR`, `RECEPTIONIST`, admin | `sendPatientChatMessage()` |
| `POST` | `/patient-chat/conversations/:id/escalate` | Escalate to a department/higher level `{ reason? }`. | Participant | `escalatePatientConversation()` |
| `POST` | `/patient-chat/conversations/:id/close` | Close the conversation. | Participant | `closePatientConversation()` |

---

## 26. AI Chatbot — `/api/v1/chatbot`

| Method | Path | Purpose | Roles | Client usage (`chat.service.ts`) |
|---|---|---|---|---|
| `POST` | `/chatbot/conversations` | Start an AI support session. | Any authenticated | `startChatbotSession()` |
| `POST` | `/chatbot/conversations/:id/messages` | Send a message to the bot `{ content }` → AI reply. | Any authenticated | `sendChatbotMessage()` |
| `GET` | `/chatbot/conversations/:id` | Fetch session message history. | Owner | `getChatbotHistory()` |
| `POST` | `/chatbot/conversations/:id/feedback` | Rate bot reply `{ rating, comment? }`. | Owner | `sendChatbotFeedback()` |

---

## 27. Uploads — `/api/v1/uploads`

| Method | Path | Purpose | Roles | Client usage (`platform.service.ts`) |
|---|---|---|---|---|
| `POST` | `/uploads/presign` | Request a presigned S3 upload `{ filename, mimeType, sizeBytes, context }` → returns upload URL + `fileId`. | Any authenticated | `presignUpload()` — every file-pick flow (patient documents, avatars, lab reports) |
| `POST` | `/uploads/:id/confirm` | Confirm the upload completed (marks the file record). | Any authenticated | `confirmUpload()` |
| `GET` | `/uploads/:id/download-url` | Get a signed download URL `{ url }`. | Any authenticated | `getDownloadUrl()` |
| `DELETE` | `/uploads/:id` | Delete an upload. | Owner/admin | `deleteUpload()` |

> The avatar & bulk-import endpoints delegate to this module — use `presign` with `context: "AVATAR"` for profile pictures until `/users/:id/avatar` is wired.

---

## 28. Audit — `/api/v1/audit` (admin)

| Method | Path | Purpose | Roles | Client usage (`platform.service.ts`) |
|---|---|---|---|---|
| `GET` | `/audit/logs?action=&entityType=&search=&page=` | Search audit logs (FRD 29.21). | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `searchAuditLogs()` — audit explorer |
| `GET` | `/audit/logs/:entityType/:entityId` | Full history for one entity (e.g. `/audit/logs/Patient/{{patientId}}`). | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `getEntityHistory()` |
| `POST` | `/audit/export` | Export logs (async job) `{ filters? }` → `{ jobId }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `exportAuditLogs()` |
| `GET` | `/audit/anomalies?severity=` | List detected anomalies. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `listAuditAnomalies()` |

---

## 29. Settings — `/api/v1/settings`

| Method | Path | Purpose | Roles | Client usage (`platform.service.ts`) |
|---|---|---|---|---|
| `GET` | `/settings/hospital-profile` | Fetch hospital profile (name, address, contact, logo). | Any authenticated | `getHospitalProfile()` — settings page |
| `PATCH` | `/settings/hospital-profile` | Update hospital profile. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `updateHospitalProfile()` |
| `GET` | `/settings/business-rules` | List business rules (e.g. discount approval threshold). | Any authenticated | `getBusinessRules()` |
| `PATCH` | `/settings/business-rules/:key` | Update a rule `{ value }` (e.g. `discount-approval-threshold`). | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `updateBusinessRule()` |
| `GET` | `/settings/feature-flags` | List feature flags (e.g. `ai-prescriptions`). | Any authenticated | `listFeatureFlags()` — drives UI feature toggles |
| `PATCH` | `/settings/feature-flags/:key` | Toggle a flag `{ isEnabled }`. | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `toggleFeatureFlag()` |
| `PUT` | `/settings/integrations/:provider` | Store integration credentials `{ config }` (e.g. `SMS`, `EMAIL`). | `SUPER_ADMIN`, `HOSPITAL_ADMIN` | `setIntegrationCredential()` |
| `GET` | `/settings/integrations` | List configured integrations (masked). | Any authenticated | `getIntegrationCredentials()` |

---

## 30. Quick Reference — Stubs & Caveats

| Endpoint | Status |
|---|---|
| `POST /api/v1/users/:id/avatar` | ⚠️ Registered but returns **501 `DEPENDENCY_FAILURE`** (S3 not wired). Use `/uploads/presign` with `context: "AVATAR"`. |
| `POST /api/v1/users/bulk-import` | ⚠️ Registered but returns **501 `DEPENDENCY_FAILURE`** (CSV job not wired). |
| Drug catalog | ❌ No public `/drugs` endpoint yet — prescription `drugId` is free text on the client. |
| `POST /api/v1/payments/webhook` | ✅ Public (no cookie) — HMAC-signed callback from the payment gateway. |

## 31. Suggested Postman test order (happy path)

1. **Auth → Sign In** (admin@hospital.com / Admin@123) — cookie saved automatically.
2. **Departments → Create** → copy `id` → `{{departmentId}}`.
3. **Doctors → Create** → `{{doctorId}}`.
4. **Patients → Register** → `{{patientId}}`.
5. **Appointments → Book** → `{{appointmentId}}`.
6. **OPD** → vitals → start consultation → diagnosis → close (optionally refer to IPD).
7. **IPD → Admit** → `{{admissionId}}` → rounds/vitals → discharge → summary.
8. **Billing → Create bill** → `{{billId}}` → discount/insurance → finalize → **Payments → Record payment**.
9. **Pharmacy / Lab / Prescriptions / AI Prescriptions** — clinical workflow.
10. **Reports → Generate** → poll `{{reportJobId}}`.
